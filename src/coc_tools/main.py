from dotenv import find_dotenv, load_dotenv
load_dotenv(find_dotenv())


def main():
    ...
    # start the API to [v1, ingest url.. v2, create game] and show stats

    # game flow:
        # create session
        # create game and return url
        # monitor game in thread
        # show game/session stats

    # call api with src
    # call api with ui

